---
summary: "CLI reference for `AGENT sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
---

# `AGENT sessions`

List stored conversation sessions.

```bash
AGENT sessions
AGENT sessions --active 120
AGENT sessions --json
```

